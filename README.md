# Java program to implement linked

## method implemented are the following:

- insertAtFront()
- insertAtBack()
- printlist
- isSorted() check if the list is sorted
- merge() method used to merge two list
- concatenation of a list
